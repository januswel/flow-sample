{
  "name": "flowtype-generics",
  "version": "0.0.0",
  "description": "flowtype generics sample",
  "main": "src/index.js",
  "scripts": {
    "build": "NODE_ENV=production webpack --optimize-minimize",
    "build:watch": "NODE_ENV=development babel src -d dist -w",
    "test": "flow; test $? -eq 0 -o $? -eq 2",
    "test:watch": "nodemon --watch src --exec npm test"
  },
  "author": "janus_wel",
  "license": "MIT",
  "devDependencies": {
    "babel-loader": "^6.2.4",
    "babel-plugin-syntax-flow": "^6.8.0",
    "babel-plugin-transform-flow-strip-types": "^6.8.0",
    "babel-preset-es2015": "^6.9.0",
    "babel-preset-stage-0": "^6.5.0",
    "flow-bin": "^0.26.0",
    "nodemon": "^1.9.2",
    "webpack": "^1.13.1"
  }
}
